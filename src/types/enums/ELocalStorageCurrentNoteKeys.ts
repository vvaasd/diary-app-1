export enum ELocalStorageCurrentNoteKeys {
  header = 'currentNote_header',
  textArea = 'currentNote_textArea',
  date = 'currentNote_date',
  emojiIndex = 'currentNote_emojiIndex',
  tagsInput = 'currentNote_tagsInput',
  tags = 'currentNote_tags',
  imageInfo = 'currentNote_imageInfo',
}
