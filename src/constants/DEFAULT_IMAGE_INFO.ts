import defaultImageSrc from '@/assets/img/theme-image.jpg';
import { ImageInfoType } from '@/types';

export const DEFAULT_IMAGE_INFO: ImageInfoType = {
  id: 'default_image',
  alt: 'градиент',
  src: defaultImageSrc,
  orientation: 'horizontal',
};
